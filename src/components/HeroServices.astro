<div class="">
    <main class="main-hero-services">
            <h1>Ingeniería <span class="">Web</span></h1>
            <p>Impulsa tu negocio con sitios web a medida, diseñados para cautivar a tus clientes y optimizar tu presencia digital.
              Nuestra ingeniería web combina innovación, diseño funcional y tecnología de vanguardia.
              Creamos experiencias en línea seguras, escalables y llenas de valor.</p>
    </main>
</div>     

  <style>

    
    .space-background {
      position: relative;
      width: 100%;
      height: 600px; /* O ajusta según necesites */
      background: radial-gradient(circle, #000428, #1c1b1b); /* Degradado espacial */
      overflow: hidden;
      padding: 18%;
      color: #fff;
    }
  
    /* Estrellas pequeñas */
    .space-background::before {
      content: "";
      position: absolute;
      top: 0;
      left: 0;
      width: 200%;
      height: 200%;
      background: radial-gradient(2px, rgba(255, 255, 255, 0.8), transparent) 0 0,
        radial-gradient(2px, rgba(255, 255, 255, 0.6), transparent) 50px 50px;
      background-size: 100px 100px;
      animation: move-stars 30s linear infinite;
      opacity: 0.7;
    }
  
    /* Estrellas grandes */
    .space-background::after {
      content: "";
      position: absolute;
      top: 0;
      left: 0;
      width: 200%;
      height: 200%;
      background: radial-gradient(3px, rgba(255, 255, 255, 0.9), transparent) 0 0,
        radial-gradient(3px, rgba(255, 255, 255, 0.7), transparent) 100px 100px;
      background-size: 150px 150px;
      animation: move-stars 3s linear infinite;
      opacity: 0.8;
    }
  
    /* Animación para mover las estrellas */
    @keyframes move-stars {
      from {
        transform: translate(0, 0);
      }
      to {
        transform: translate(-50px, -50px);
      }
    }
  
    /* Contenido dentro del componente */
    .space-background h1 {
      position: relative;
      color: #fff;
      font-size: 3rem;
      text-align: left;
      transform: translateY(-50%);
    }
    p {
        padding-top: 2%;
    }
  </style>
  