---
const metodologia = [
  {
    icon: "👨‍💻",
    title: "Construimos",
    description: "Desarrollamos soluciones tecnológicas innovadoras con las mejores prácticas del mercado",
    delay: "0ms"
  },
  {
    icon: "🧪",
    title: "Medimos",
    description: "Analizamos métricas y datos para evaluar el rendimiento y impacto de nuestras soluciones",
    delay: "200ms"
  },
  {
    icon: "🧠",
    title: "Aprendemos",
    description: "Extraemos insights valiosos de los resultados para mejorar continuamente nuestros procesos",
    delay: "400ms"
  },
  {
    icon: "🔄",
    title: "Iteramos",
    description: "Aplicamos mejoras constantes basadas en el aprendizaje para optimizar los resultados",
    delay: "600ms"
  }
];
---

<section class="section-methodology">
  <div class="container-methodology">
    <!-- TÍTULO PRINCIPAL -->
    <h1 class="main-title">
      Así es como <span class="italic-text">lo hacemos...</span>
    </h1>

    <!-- TARJETAS DE CADA PASO -->
    <div class="cards-container">
      {metodologia.map((paso, index) => (
        <div 
          class="methodology-card" 
          data-aos="fade-up"
          style={`animation-delay: ${paso.delay}`}
        >
          <div class="card-icon-container">
            <div class="card-icon">{paso.icon}</div>
            <div class="step-number">0{index + 1}</div>
          </div>
          <div class="card-content">
            <h3 class="card-title">{paso.title}</h3>
            <p class="card-description">{paso.description}</p>
          </div>
          <div class="card-glow"></div>
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  .section-methodology {
    background: #101010;
    color: white;
    padding: 5rem 0;
    position: relative;
    overflow: hidden;
  }

  .section-methodology::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: radial-gradient(circle at 20% 50%, rgba(16, 185, 129, 0.1) 0%, transparent 50%),
                radial-gradient(circle at 80% 50%, rgba(52, 211, 153, 0.1) 0%, transparent 50%);
    pointer-events: none;
  }

  .container-methodology {
    max-width: 1400px;
    margin: 0 auto;
    padding: 0 10rem;
    position: relative;
    z-index: 1;
  }

  /* ===== TÍTULO PRINCIPAL ===== */
  .main-title {
    color: white;
    font-size: clamp(32px, 6vw, 48px);
    text-align: center;
    margin-bottom: 4rem;
    opacity: 0;
    animation: titleFadeIn 1s ease-out 0.5s forwards;
  }

  .italic-text {
    color: #fff;
    font-style: italic;
    font-family: "Playfair-Display", serif;
    background: linear-gradient(45deg, #ffffff, #ffffff);
    background-clip: text;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  /* ===== TARJETAS ===== */
  .cards-container {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 2rem;
    max-width: 1400px;
    margin: 0 auto;
  }

  .methodology-card {
    background: linear-gradient(135deg, #1a1a1a, #2a2a2a);
    border-radius: 16px;
    padding: 2rem;
    position: relative;
    border: 1px solid #333;
    transition: all 0.6s cubic-bezier(0.4, 0, 0.2, 1);
    opacity: 0;
    transform: translateY(30px);
    animation: cardFadeIn 0.8s ease-out forwards;
    overflow: hidden;
  }

  .methodology-card:hover {
    transform: translateY(-10px) scale(1.02);
    border-color: #10B981;
    box-shadow: 0 20px 40px rgba(16, 185, 129, 0.2),
                0 0 20px rgba(16, 185, 129, 0.1);
  }

  .card-glow {
    position: absolute;
    top: -2px;
    left: -2px;
    right: -2px;
    bottom: -2px;
    background: linear-gradient(45deg, #101010, #101010, #101010);
    border-radius: 18px;
    opacity: 0;
    z-index: -1;
    transition: opacity 0.6s ease;
  }

  .methodology-card:hover .card-glow {
    opacity: 0.7;
  }

  .card-icon-container {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 1.5rem;
  }

  .card-icon {
    font-size: 3rem;
    animation: iconBounce 2s ease-in-out infinite;
    filter: drop-shadow(0 0 10px rgba(16, 185, 129, 0.3));
  }

  .step-number {
    font-size: 1rem;
    color: #666;
    font-weight: 300;
    opacity: 0.7;
  }

  .card-title {
    font-size: 1.5rem;
    font-weight: 700;
    margin-bottom: 1rem;
    color: #fff;
    background: linear-gradient(45deg, #fff, #f0f0f0);
    background-clip: text;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .card-description {
    font-size: 0.9rem;
    color: #ccc;
    line-height: 1.6;
    font-family: "Open Sans", sans-serif;
  }

  /* ===== ANIMACIONES ===== */
  @keyframes titleFadeIn {
    from {
      opacity: 0;
      transform: translateY(-20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes lineProgress {
    to {
      width: 100%;
    }
  }

  @keyframes dotActivate {
    to {
      background: linear-gradient(45deg, #10B981, #34D399);
      border-color: #10B981;
      color: white;
      box-shadow: 0 0 20px rgba(16, 185, 129, 0.5);
    }
  }

  @keyframes cardFadeIn {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes iconBounce {
    0%, 100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(-5px);
    }
  }

  /* ===== RESPONSIVE DESIGN ===== */
  @media screen and (max-width: 768px) {
    .section-methodology {
      padding: 3rem 0;
    }

    .container-methodology {
      padding: 0 1rem;
    }

    .main-title {
      margin-bottom: 3rem;
    }

    .cards-container {
      grid-template-columns: 1fr;
      gap: 1.5rem;
    }

    .methodology-card {
      padding: 1.5rem;
    }

    .methodology-card:hover {
      transform: translateY(-5px) scale(1.01);
    }

    .card-icon {
      font-size: 2.5rem;
    }

    .card-title {
      font-size: 1.3rem;
    }

    .card-description {
      font-size: 0.9rem;
    }
  }

  @media screen and (max-width: 480px) {
    .methodology-card {
      padding: 1.25rem;
    }

    .card-icon {
      font-size: 2.25rem;
    }

    .process-line {
      justify-content: center;
    }
  }

  /* Reducir movimiento para usuarios sensibles */
  @media (prefers-reduced-motion: reduce) {
    .methodology-card:hover {
      transform: none;
    }

    .card-icon {
      animation: none;
    }

    .animated-line::before,
    .process-dot {
      animation: none;
    }
  }
</style>

<script>
  // Intersection Observer para activar animaciones al hacer scroll
  const observerOptions = {
    threshold: 0.2,
    rootMargin: '0px 0px -50px 0px'
  };

  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.style.animationPlayState = 'running';
      }
    });
  }, observerOptions);

  // Observar todas las tarjetas
  document.querySelectorAll('.methodology-card').forEach(card => {
    observer.observe(card);
  });

  // Efecto de parallax sutil en el fondo
  window.addEventListener('scroll', () => {
    const scrolled = window.pageYOffset;
    const section = document.querySelector('.section-methodology');
    
    if (section) {
      const rect = section.getBoundingClientRect();
      const speed = scrolled * 0.5;
      
      if (rect.top < window.innerHeight && rect.bottom > 0) {
        section.style.transform = `translateY(${speed * 0.1}px)`;
      }
    }
  });
</script>