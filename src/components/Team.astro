---
// Ejemplo minimalista de un carrusel horizontal automático en Astro.
// Ajusta la información de tu equipo según necesites.
const equipo = [
  { nombre: 'Hernán Estupiñán', rol: 'Marketing Manager', foto: '/img/team/hernan-estupinan.webp' },
  { nombre: 'Laura Diáz', rol: 'Data Analitycs Specialist', foto: '/img/team/laura-diaz.webp' },
  { nombre: 'Nicolás Guzmán', rol: 'Development Manager', foto: '/img/team/nicolas-guzman.webp' },
  { nombre: 'Rubén Navarro', rol: 'UX/UI Director', foto: '/img/team/ruben-navarro.webp' },
  { nombre: 'Felipe Estupiñán', rol: 'Project Manager', foto: '/img/team/felipe-estupinan.webp' },
];
---

<div class="contenedor">
  <h1 style="white-space: pre-wrap;">🦊 Nuestra<span> Manada</span></h1>
  <div class="scroll">
    {equipo.map((persona) => (
      <article class="miembro">
        <img src={persona.foto} alt={`Foto de ${persona.nombre}`} />
        <h2>{persona.nombre}</h2>
        <p>{persona.rol}</p>
      </article>
    ))}
    {/* Duplicamos el contenido para un efecto de bucle continuo */}
    {equipo.map((persona) => (
      <article class="miembro">
        <img src={persona.foto} alt={`Foto de ${persona.nombre}`} />
        <h2>{persona.nombre}</h2>
        <p>{persona.rol}</p>
      </article>
    ))}
  </div>
</div>

<style>
  .contenedor {
    width: 100%;
    overflow: hidden;
    background: radial-gradient(circle, #202020 0%, #101010 100%);
    color: #fff;
    padding: 0% 18% 12% 18%;
    box-sizing: border-box;
  }

  h1 {
            color: white;
            font-size: 42px;
            max-width: 100%;
            line-height: normal;
            padding: 10rem 0rem;
            text-align: center;
        }

        p {
            color: #ffffff;
            font-size: 16px;
            max-width: 100%;
        }

        span {
            color: #ffffff;
            font-style: italic;
            font-family: "Playfair-Display" !important;
            font-size: 42px;
        }

  .scroll {
    display: flex;
    gap: 3.5rem;
    /* La idea es ocupar el doble de espacio para el bucle */
    width: 200%;
    animation: desplazar 20s linear infinite;
  }

  .miembro {
    min-width: 180px; /* Ajusta según el tamaño de foto o texto */
    text-align: center;
    font-family: sans-serif;
  }

  img {
    width: 180px;
    height: 180px;
    object-fit: cover;
    border-radius: 50%;
    display: block;
    margin: 0 auto 0.5rem;
  }

  @keyframes desplazar {
    0%   { transform: translateX(0); }
    100% { transform: translateX(-50%); }
  }

  h2{
    font-size: 1.3rem;
    padding-top: 1.3rem;
  }

  p{
    font-size: 0.8rem;
    color:#8f8e8e;
  }

   /** Mobile Responsive */

   @media screen and (max-width: 768px) {

    .contenedor{
      padding: 5rem 1.5rem;
    }
    
    .scroll {
    animation: desplazar 10s linear infinite;
    width: 700%;
    }

    h2{
    font-size: 1.3rem;
    }

    h1{
      padding: 5rem 0rem;

    }


   }



</style>
