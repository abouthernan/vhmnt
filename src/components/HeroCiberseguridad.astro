<section class="hero-section">
  <div class="hero-container">
      <h1 class="gradient-heading">Protegemos el activo más<br /> importante de tu empresa,<br /><span class="gradient-accent"> la información.</span></h1>
      <!-- <p>
        Protegemos tu negocio de amenazas digitales con soluciones de seguridad<br/>
        personalizadas. Tecnología de vanguardia y expertos dedicados para<br/>
        salvaguardar tus datos y reputación.-->
      </p>
      <!-- Call to Action -->
    <div class="cta-container">
      <a href="https://api.whatsapp.com/send/?phone=%2B573133846633&text&type=phone_number&app_absent=0" class="cta-gradient">
        <!-- <svg class="cta-icon" viewBox="0 0 24 24">
          <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-2 15l-5-5 1.41-1.41L10 14.17l7.59-7.59L19 8l-9 9z"/>
        </svg>-->
        Solicita una Auditoría Gratuita
      </a>
    </div>
  </div>
</section>

<style>


  .hero-section {
            background:#0c0c0c;
            overflow: hidden;
            padding: 10% 18% 5% 18%;
            position: relative;
            max-width: 100%;
            background: radial-gradient(circle at 30% 20%, #1d1d1e 0%, #0c0c0c 50%);
            animation: breathe-gradient 8s ease-in-out infinite;
            color: white;
        }

  .hero-container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 0;
        }
  

  .gradient-heading {
    font-size: 3.2rem;
    font-weight: 400;
    max-width: 100%;
    line-height: 1.2;
    padding-bottom: 2%;
    margin: 0;
    color: #ffffff;
    
    /* Gradiente moderno 
    background: linear-gradient(135deg, #00f5ff, #00ff88, #0080ff);
    background-size: 200% 200%;
    background-clip: text;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;*/
    
    /* Animación sutil */
    animation: gradient-shift 4s ease-in-out infinite;
  }

  .gradient-accent {
    font-style: italic;
    font-family: "Playfair-Display" !important;
    font-size: 1.2em;
    
    /* Gradiente complementario para el span */
    background: linear-gradient(135deg, #00ff88, #00f5ff);
    background-clip: text;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }



  /* Efecto de glow opcional */
  .gradient-heading.with-glow {
    filter: drop-shadow(0 0 20px rgba(0, 245, 255, 0.3));
  }

  p {
    color: #ffffff;
    font-size: 18px;
    max-width: 100%;
  }

  /* Mobile Responsive */
  @media screen and (max-width: 768px) {
    .gradient-heading {
      font-size: 2em;
      max-width: 100%;
      text-align: center;
      line-height: normal;
    }

    .gradient-accent {
      font-size: 1.2em;
    }

    p {
      font-size: 18px;
      max-width: 100%;
      text-align: center;
      padding-top: 2%;
    }
  }


  /* CTA Styles */
.cta-container {
  margin-top: 2.5rem;
  text-align: left;
  position: relative;
  z-index: 5;
}

.cta-gradient {
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
  padding: 1.2rem 2.5rem;
  font-size: 1.1rem;
  font-weight: 600;
  text-decoration: none;
  border-radius: 50px;
  background: linear-gradient(135deg, #00f5ff, #00ff88);
  color: #000;
  transition: all 0.3s ease;
  border: none;
  cursor: pointer;
  position: relative;
  overflow: hidden;
}

.cta-gradient:hover {
  transform: translateY(-3px);
  box-shadow: 0 12px 35px rgba(0, 245, 255, 0.4);
}

.cta-gradient::before {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(90deg, transparent, rgba(255,255,255,0.3), transparent);
  transition: left 0.5s;
}

.cta-gradient:hover::before {
  left: 100%;
}

.cta-icon {
  width: 20px;
  height: 20px;
  fill: currentColor;
}

/* Mobile CTA */
@media screen and (max-width: 768px) {
  .cta-container {
    margin-top: 1.5rem;
    text-align: center;
  }

  .cta-gradient {
    padding: 1rem 2rem;
    font-size: 1rem;
  }
}



</style>