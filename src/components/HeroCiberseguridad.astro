<section class="section-hero">
  <div class="container-hero">
    <div class="gradient-heading">
      <h1> Protegemos el activo<br class="break-mobile" /> más importante de tu empresa,<br class="break-mobile" />
      <span class="dynamic-text">
        <span class="text-option gradient-accent">tus empleados.</span>
        <span class="text-option gradient-accent">tu información.</span>
        <span class="text-option gradient-accent">tu reputación.</span>
      </span>
      </h1>
    </div>
    <!-- Call to Action -->
    <div class="cta-container">
      <a
        href="https://wa.me/+573017831450"
        class="cta-gradient"
      >
        Hablar con un experto
      </a>
      <a
        href="https://cal.com/afestupinan"
        class="cta-outline"
      >
        Agenda una asesoría
      </a>
    </div>
  </div>
</section>

<style>

    /* Desktop */
.break-desktop { display: block; }  /* Visible en desktop */
.break-mobile { display: none; }    /* Oculto en desktop */

  .section-hero {
    background: #101010;
    color: white;
    padding-top: 10rem;
    padding-bottom: 5rem;
    position: relative;
  }

   .container-hero {
    max-width: 1400px;
    margin: 0 auto;
    padding: 0;
  }

  span{
    font-size: 1em;
  }

    .section-hero::before {
    content: '';
    position: absolute;
    top: 40%;
    left: 30%;
    width: 300px;
    height: 300px;
    transform: translate(-50%, -50%);
    background: radial-gradient(
        circle,
        rgba(0, 245, 255, 0.04) 0%,
        rgba(0, 245, 255, 0.02) 50%,
        transparent 70%
    );
    border-radius: 50%;
    animation: radialExpand 5s ease-in-out infinite;
    z-index: 1;
}

@keyframes radialExpand {
    0%, 100% {
        transform: translate(-50%, -50%) scale(0.8);
        opacity: 0.6;
    }
    50% {
        transform: translate(-50%, -50%) scale(1.5);
        opacity: 1;
    }
}

  /* Gradient Heading Styles */
  .gradient-heading {
    font-size: 3rem;
    font-weight: 700;
    line-height: 1.1;
    margin-bottom: 2rem;
    position: relative;
    z-index: 2;
  }

  .gradient-accent {
    background: linear-gradient(135deg, #00f5ff, #00ff88);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    font-size: 1.1em;
    line-height: inherit;
    vertical-align: baseline;   
  }

  /* Texto dinámico */
  .dynamic-text {
    display: inline-block;
    position: relative;
    vertical-align: top;
    line-height: 1.1;
  }

  .text-option {
    opacity: 0;
    position: absolute;
    top: 0;
    left: 0;
    white-space: nowrap;
    animation: textRotate 6s infinite;
    transform: translateX(10px);
    vertical-align: baseline;
  
  }

  .text-option:nth-child(1) { animation-delay: 0s; }
  .text-option:nth-child(2) { animation-delay: 2s; }
  .text-option:nth-child(3) { animation-delay: 4s; }

  @keyframes textRotate {
    0%, 20% {
        opacity: 1;
        transform: translateX(0);
    }
    25%, 100% {
        opacity: 0;
        transform: translateY(-10px);
    }
  }

 /* CTA Styles */
  .cta-container {
    margin-top: 2.5rem;
    text-align: left;
    position: relative;
    z-index: 5;
  }

  .cta-gradient {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 1.2rem 2.5rem;
    font-size: 1.1rem;
    font-weight: 600;
    text-decoration: none;
    border-radius: 50px;
    background: linear-gradient(135deg, #00f5ff, #00ff88);
    color: #000;
    transition: all 0.3s ease;
    border: none;
    cursor: pointer;
    position: relative;
    overflow: hidden;
  }

  .cta-gradient:hover {
    transform: translateY(-3px);
    box-shadow: 0 12px 35px rgba(0, 245, 255, 0.4);
  }

  .cta-gradient::before {
    content: "";
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(
      90deg,
      transparent,
      rgba(255, 255, 255, 0.3),
      transparent
    );
    transition: left 0.5s;
  }

  .cta-gradient:hover::before {
    left: 100%;
  }

  .cta-icon {
    width: 20px;
    height: 20px;
    fill: currentColor;
  }

  /** Mobile Responsive */

  @media screen and (max-width: 768px) {
    .gradient-heading {
      font-size: 3rem; /* Reducir más el tamaño */
      max-width: 100%;
      text-align: center;
      line-height: 1;
      padding: 0 0.5rem; /* Agregar padding lateral */
    }

    .gradient-heading h1 {
      overflow: hidden; /* Evitar desbordamiento */
    }

    .gradient-accent {
      font-size: 1.1em; /* Reducir un poco */
    }

    .section-hero {
      padding-top: 8rem; /* Reducir padding superior */
    }

    .container-hero {
      padding: 0 1rem; /* Agregar padding al contenedor */
    }

    .cta-container {
      margin-top: 1.5rem;
      text-align: center;
    }

    .cta-gradient {
      padding: 1rem 2rem;
      font-size: 1rem;
    }

     span{
    font-size:1em;
  }

    /* Texto dinámico optimizado para móvil */
    .dynamic-text {
      height: 1.3em; /* Altura más compacta */
      display: inline-block;
      width: 100%; /* Asegurar que ocupe todo el ancho disponible */
      overflow: hidden; /* Evitar desbordamiento */
   
    }

    .text-option {
      white-space: nowrap; /* NO permitir salto de línea */
      position: absolute;
      top: 0;
      left: 0;
      font-size: 0.75em; /* Reducir tamaño del texto dinámico */
      width: 100%;
      text-align: center; /* Alinear a la izquierda */
      line-height: 1.1;

    }

  


  }


  /* Mobile Responsive */
  @media screen and (max-width: 768px) {
    .desktop-only {
      display: none;
    }
    
    .mobile-only {
      display: block;
    }

    .gradient-heading {
      font-size: 1rem;
      max-width: 100%;
      text-align: center;
      line-height: normal;
      text-align: center;
    }

    .gradient-accent {
      font-size: 1.2em;
    }

    p {
      font-size: 18px;
      max-width: 100%;
      text-align: center;
      padding-top: 2%;
    }
  }

  .cta-outline {
      padding: 1rem 2rem;
      font-size: 1rem;
      width: 100%;
      justify-content: center;
      max-width: 280px;
    }

  /* CTA Styles */
  .cta-container {
    margin-top: 2.5rem;
    text-align: left;
    position: relative;
    z-index: 5;
    display: flex;
    gap: 1rem;
    align-items: center;
  }

  .cta-gradient {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 1.2rem 2.5rem;
    font-size: 1.1rem;
    font-weight: 600;
    text-decoration: none;
    border-radius: 50px;
    background: linear-gradient(135deg, #00f5ff, #00ff88);
    color: #000;
    transition: all 0.3s ease;
    border: none;
    cursor: pointer;
    position: relative;
    overflow: hidden;
  }

  .cta-gradient:hover {
    transform: translateY(-3px);
    box-shadow: 0 12px 35px rgba(0, 245, 255, 0.4);
  }

  .cta-gradient::before {
    content: "";
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(
      90deg,
      transparent,
      rgba(255, 255, 255, 0.3),
      transparent
    );
    transition: left 0.5s;
  }

  .cta-gradient:hover::before {
    left: 100%;
  }

  /* CTA Outline Styles */
  .cta-outline {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 1.2rem 2.5rem;
    font-size: 1.1rem;
    font-weight: 600;
    text-decoration: none;
    border-radius: 50px;
    background: transparent;
    color: #a0a1a1;
    border: 1px solid #a0a1a1;
    transition: all 0.3s ease;
    cursor: pointer;
    position: relative;
    overflow: hidden;
  }

  .cta-outline:hover {
    transform: translateY(-3px);
    background: rgba(0, 245, 255, 0.1);
    box-shadow: 0 8px 25px rgba(0, 245, 255, 0.2);
    border-color: #00ff88;
    color: #00ff88;
  }

  .cta-outline::before {
    content: "";
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(
      90deg,
      transparent,
      rgba(0, 245, 255, 0.1),
      transparent
    );
    transition: left 0.5s;
  }

  .cta-outline:hover::before {
    left: 100%;
  }

  .cta-icon {
    width: 20px;
    height: 20px;
    fill: currentColor;
  }

    /* Mobile */
  .break-desktop { display: none; }   /* Oculto en mobile */
  .break-mobile { display: block; }   /* Visible en mobile */



  /* Mobile CTA */
  @media screen and (max-width: 768px) {
    .cta-container {
      margin-top: 1.5rem;
      text-align: center;
      flex-direction: column;
      gap: 0.8rem;
    }

    .cta-gradient {
      padding: 1rem 2rem;
      font-size: 1rem;
      width: 100%;
      justify-content: center;
      max-width: 280px;
    }

    .cta-outline {
      padding: 1rem 2rem;
      font-size: 1rem;
      width: 100%;
      justify-content: center;
      max-width: 280px;
    }
  }

  @media screen and (max-width: 480px) {
    .cta-container {
      gap: 0.6rem;
    }

    .cta-gradient,
    .cta-outline {
      padding: 0.9rem 1.5rem;
      font-size: 0.95rem;
      max-width: 250px;
    }

      /* Mobile */
  .break-desktop { display: none; }   /* Oculto en mobile */
  .break-mobile { display: block; }   /* Visible en mobile */


  }
  
</style>

